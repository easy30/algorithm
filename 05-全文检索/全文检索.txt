/**
 * 
 * 在csv文件解析的基础上增加简单全文检索功能。
 * 输入一个字符串text，然后返回包含此字符串中每一个字符的记录。
   如：text=中国人 ，则搜索结果记录同时包含“中”、“国”、“人”即可。
   
 *
 */
public interface Table {
    

    public  String[][] search(String text);

}
